--- 
 - hosts: mywebserver 
   become: yes 
   tasks:
     - name: install multi package
       apt:
         name: "{{item}}"
         state: present 
       loops: 
         - get 
         - tree 
         - wget